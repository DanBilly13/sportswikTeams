{
  "name": "Sporstwik Avatars",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@figma/plugin-typings": "^1.119.0",
    "typescript": "^5.4.0"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build:ts": "tsc -p .",
    "build:assets": "node scripts/copy-ui.mjs",
    "build": "npm run clean && npm run build:ts && npm run build:assets",
    "watch": "npm run build && tsc -w -p .",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "figma-plugin-ds": "^1.0.1"
  }
}
